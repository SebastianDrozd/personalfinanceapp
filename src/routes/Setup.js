import React from 'react'
import { Outlet, Link } from "react-router-dom";
const Setup = () => {
    return (
        <div>
            setup
            <Outlet />
        </div>
    )
}

export default Setup
